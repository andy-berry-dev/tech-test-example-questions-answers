type User {
    id: String!
    name: String!
}

type Question {
    id: String!
    text: String!
}

type Query {
    users: [User!]!
    questions: [Question!]!
}

type Mutation {
    addQuestion(text: String!): Question!
}
